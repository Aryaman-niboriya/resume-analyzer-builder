{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/resume-analyzer run build",
  "outputDirectory": "frontend/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
